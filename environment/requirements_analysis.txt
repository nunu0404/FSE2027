# Tier 1 (re-run analyses) and Tier 2 (re-render images).
# CPU only; no GPU or model weights required.
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
statsmodels>=0.14
PyYAML>=6.0

# Tier 2 rendering
Pillow>=10.0
pygments>=2.17

# tree-sitter grammars are vendored in code/01_rendering/vendor/ at the pinned
# versions used by the original runs (tree_sitter 0.25.2, java 0.23.5,
# python 0.25.0, cpp 0.23.4). Install only if you cannot use the vendored copy.
# tree_sitter==0.25.2
