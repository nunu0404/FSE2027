# Historical alignment limitations

The historical Swiss/Elo and new direct-pairwise runs differ in sampling, renderer, variant generator, model provenance, and aggregation. Numeric effect sizes are not pooled. Only ordinal direction is compared. The historical report states 200 bases, while the available image-only Elo file contains 160 items per variant; the artifact count is reported.
